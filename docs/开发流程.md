# 开发流程与模块说明

本文记录项目当前已经实现的结构、关键文件用途和功能开发步骤。每次新增或调整功能时都应同步更新本文，避免代码已经变化而文档仍描述旧行为。

## 当前请求分层

```text
HTTP 请求
  → Router：解析参数、注入依赖、声明状态码、转换 HTTP 异常
  → Schema：校验请求并限制响应字段
  → Service：执行业务规则、查询、密码处理和事务提交
  → Model：描述 ORM 字段与数据库约束
  → Session / Engine：管理事务和数据库连接
  → SQLite（开发）或 PostgreSQL（生产）
```

Router 不直接编写 SQL，Service 不处理 HTTP 响应，Model 不负责请求参数校验。保持边界清晰可以让业务逻辑独立测试，也方便未来加入认证和权限控制。

用户名和邮箱在用户 Service 中统一去除首尾空格并转为小写保存。注册、资料更新和登录
查询使用 `func.lower()` 兼容历史混合大小写数据，因此 `Alice`、`ALICE` 和 `alice`
视为同一个用户名，邮箱同理。

## JWT 认证与 AJAX 表单

登录调用链为 `layout.html/login.html → auth.js → api.js → POST /api/auth/token → api_auth.py → services/auth.py → users 表`。成功后前端把短期 Token 保存到 `localStorage`。

发帖调用链为 `post_form.html → posts.js → api.js（Bearer Header）→ api_posts.py → dependencies/auth.py → services/posts.py → posts 表`。新增、修改和删除帖子仅允许管理员；用户资料修改和删除仅允许本人或管理员；新增帖子的 `user_id` 来自 JWT 对应数据库用户，不接受客户端指定。右上角登录和注册按钮只打开 Bootstrap 模态框，独立 `/login`、`/register` 页面仍作为可访问的备用入口。

管理员通过交互式管理命令创建，密码输入不会回显：

```powershell
uv run python -m app.cli create-admin
```

命令使用当前 `ENV` 对应的数据库，运行前应确认环境和迁移已经就绪。公开 `/api/users` 注册接口始终创建普通用户，不能通过请求体传入 `is_admin`。

本次新增或修改的关键文件：

| 文件 | 用途 |
| --- | --- |
| `app/schemas/auth.py` | Bearer Token 响应契约 |
| `app/services/auth.py` | 显式封装密码哈希/校验与 JWT 创建/验证四个安全函数 |
| `app/dependencies/auth.py` | 当前用户和管理员依赖，统一 401/403 |
| `app/routers/api_auth.py` | OAuth2 表单登录接口 |
| `app/routers/api_posts.py` | 要求管理员 Token 才能新增、编辑或删除帖子 |
| `app/cli.py` | 交互式创建管理员，复用用户 Service 和 Argon2 哈希 |
| `app/static/js/api.js` | 公共 `jquery.ajax` 与 Bearer Header 封装 |
| `app/static/js/auth.js` | 登录、注册和退出交互 |
| `app/static/js/posts.js` | 发布、编辑帖子交互 |
| `app/static/js/forms.js` | 搜索和个人资料表单 AJAX 交互 |
| `app/templates/layout.html` | 导航认证模态框和 ES module 入口 |
| `tests/test_auth.py` | JWT 与管理员发帖授权回归测试 |

本功能不改变数据库结构，不需要新增 Alembic Migration。

## Refresh Token 与空闲过期

认证现在使用短期 Access Token 加服务端 Refresh Session：

| 配置 | 作用 |
| --- | --- |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Access Token 有效期，例如调试设为 `2` 或 `5` |
| `REFRESH_TOKEN_EXPIRE_MINUTES` | Refresh Session 绝对最长寿命，持续操作也不能超过此时间 |
| `REFRESH_IDLE_TIMEOUT_MINUTES` | 连续无受保护请求的最大空闲时间，超过后 Refresh Token 失效 |
| `AUTH_COOKIE_SECURE` | 本地 HTTP 调试设 `false`，生产 HTTPS 必须设 `true` |

登录接口把 Access Token 返回 JSON，并把随机 Refresh Token 写入 `HttpOnly` Cookie。服务端只保存 Refresh Token 的 SHA-256 哈希。每次受保护请求会更新 `last_activity_at`；Access Token 临近或已经过期时，前端 AJAX 调用 `/api/auth/refresh` 轮换 Cookie 并重试。连续空闲超过配置值或达到绝对过期时间后必须重新登录。

新增文件和职责：

| 文件 | 用途 |
| --- | --- |
| `app/models/refresh_session.py` | Refresh Session 表模型 |
| `app/services/auth.py` | 创建、轮换、活动时间更新和撤销 Refresh Session |
| `app/routers/api_auth.py` | `/refresh` 换 Token、`/logout` 清 Cookie |
| `app/dependencies/auth.py` | 认证成功后更新最后活动时间 |
| `migrations/versions/20260724_01_create_refresh_sessions.py` | 创建 Refresh Session 表 |
| `app/static/js/api.js` | 401 时刷新 Access Token 并保存新值 |

调试时可以在 `.env.development` 或当前 PowerShell 会话中覆盖时间：

```powershell
$env:ACCESS_TOKEN_EXPIRE_MINUTES = "2"
$env:REFRESH_IDLE_TIMEOUT_MINUTES = "5"
$env:REFRESH_TOKEN_EXPIRE_MINUTES = "30"
```

配置优先级是系统环境变量高于环境文件，因此不用修改代码即可测试短时过期。修改配置后要重启 FastAPI 进程；已经签发的旧 Token 不会自动改变过期时间。

## 帖子富文本编辑器

帖子编辑页使用 Toast UI Editor，正文以 Markdown 字符串保存到现有 `posts.content`，
因此没有数据库结构变化。编辑器支持所见即所得、Markdown、图片和 fenced code block；
JSON、JavaScript、Bash 示例分别使用 `json`、`javascript`、`bash` 语言标记。

图片调用 `POST /api/posts/images` 上传，仅管理员可用。后端按文件魔数允许 PNG、JPEG、
GIF、WebP，单个文件不超过 5 MB，保存到 `app/media/post_images/`，正文只保存随机文件名
对应的 `/media/post_images/...` URL。相关文件包括：

| 文件 | 用途 |
| --- | --- |
| `app/services/images.py` | 图片大小、文件签名和随机文件名校验 |
| `app/routers/api_posts.py` | 管理员图片上传接口 |
| `app/static/js/posts.js` | 初始化编辑器、上传图片和提交 Markdown |
| `app/templates/post_form.html` | 编辑器容器和 CDN 资源 |
| `app/templates/post.html` | 使用 Toast UI Viewer 渲染正文和代码块 |

VSCode 调试 JWT 管理员链路请阅读 `docs/VSCode调试JWT管理员链路.md`，该文按登录签发、
Bearer Header、Token 验证、数据库用户查询、`require_admin` 和发帖 Router 的顺序说明断点与变量观察方法。

## 应用与配置模块

| 文件 | 用途 |
| --- | --- |
| `app/main.py` | 创建 FastAPI 应用、挂载静态与媒体目录、注册 Router、处理全局 404 |
| `app/core/config.py` | 按 `ENV` 加载公共及环境专用配置，校验数据库类型和安全配置 |
| `app/templating.py` | 创建共享的 Jinja2 模板对象，并使用稳定的模板目录路径 |
| `.env` | 本机公共配置与 `SECRET_KEY`；已从 Git 跟踪中移除 |
| `.env.development` | 开发环境配置，使用 SQLite |
| `.env.production` | 生产环境非敏感配置；数据库凭据应由部署环境注入 |

配置文件自身已添加用途说明。`alembic.ini` 是例外：Alembic 在当前 Windows 环境按系统 GBK 编码读取 INI，直接写入 UTF-8 中文会导致迁移命令解析失败，因此该文件保留 ASCII 注释，对应中文解释如下：

- `script_location`：指定迁移脚本目录，`%(here)s` 表示以 `alembic.ini` 所在目录为基准。
- `prepend_sys_path`：把项目根目录加入模块搜索路径，使迁移环境能够导入 `app`。
- `sqlalchemy.url`：故意不写入 INI，由 `migrations/env.py` 从 Settings 读取，避免提交数据库凭据。
- `[loggers]` 及后续配置：只控制 Alembic CLI 日志，不影响 FastAPI 应用日志。

## 数据库模块

| 文件 | 用途 |
| --- | --- |
| `app/db/base.py` | 定义所有 ORM 模型共用的 `DeclarativeBase` 和 `MetaData` |
| `app/db/session.py` | 创建 AsyncEngine、`AsyncSessionLocal` 和 FastAPI 的异步 `get_db()` 依赖 |
| `app/models/user.py` | 定义 `users` 表字段、唯一约束、默认昵称、管理员标记和头像 URL |
| `alembic.ini` | Alembic 命令入口配置，不保存数据库密码 |
| `migrations/env.py` | 从 Settings 读取数据库地址，并把模型 MetaData 提供给 Alembic |
| `migrations/versions/20260722_01_create_users_table.py` | 首次创建 `users` 表的迁移，可通过 downgrade 删除该表 |

数据库结构变更不能只修改 Model。正常流程是修改 Model 后创建一个新的迁移文件，检查 `upgrade()` 和 `downgrade()`，再迁移目标环境。

```powershell
uv run alembic upgrade head
```

生产环境执行迁移前必须确认 `ENV=production` 和目标数据库，不能把开发 SQLite 当作生产迁移结果。

## 用户 CRUD 功能

### 相关文件

| 文件 | 用途 |
| --- | --- |
| `app/schemas/user.py` | 定义 `UserCreate`、`UserUpdate` 和 `UserResponse`，隔离密码输入与公开响应 |
| `app/services/users.py` | 规范化邮箱、生成 Argon2 密码哈希、执行 CRUD、处理事务和唯一冲突 |
| `app/routers/api_users.py` | 暴露 `/api/users` 接口，注入 Session，并把业务结果转换为 HTTP 响应 |
| `app/routers/__init__.py` | 汇总并导出 Router，供应用入口统一注册 |
| `tests/test_users.py` | 使用隔离内存 SQLite 验证 CRUD、密码安全、冲突、404 和 422 |
| `docs/用户接口.md` | 面向接口使用者说明地址、请求体、响应状态和迁移前置条件 |
| `docs/数据库设计.md` | 解释用户表字段、约束、头像策略、密码设计和暂不增加的字段 |
| `docs/数据库迁移与常用操作.md` | 说明 Alembic 生成/执行迁移、SQLite/PostgreSQL 检查备份及 Session 操作 |
| `docs/同步与异步开发指南.md` | 说明同步/异步选型、AsyncSession 配置、全链路改造和异步测试技能 |

### 创建用户调用链

```text
POST /api/users
  → UserCreate 校验 username、email、password
  → api_users.create_user() 注入 Session
  → services.users.create_user()
      → 规范化邮箱
      → 检查用户名与邮箱
      → 使用 Argon2 哈希密码
      → INSERT + COMMIT + REFRESH
  → UserResponse 过滤 password 和 hashed_password
  → 201 Created
```

数据库唯一约束仍是并发安全的最终保证。Service 中的预查询用于提前给出友好错误，`IntegrityError` 捕获用于处理两个并发请求同时通过预查询的情况。

### 当前接口

```text
POST   /api/users             创建用户
GET    /api/users             分页列出用户
GET    /api/users/{user_id}   查询用户
PATCH  /api/users/{user_id}   部分更新用户
DELETE /api/users/{user_id}   删除用户
```

当前尚未实现 JWT 和认证依赖，因此用户接口暂时公开，只适合开发环境。文档不能把未来计划中的认证描述成已经具备的能力。

JWT 与认证的待开发技术方案见 `docs/JWT与用户认证开发流程.md`。该文件是实施计划，认证完成并通过测试前不能将其中的权限能力视为项目现状。

## 页面与帖子模块现状

| 文件 | 当前用途 |
| --- | --- |
| `app/routers/pages.py` | 提供首页、帖子列表/详情、新建/编辑表单、登录、注册和用户资料 GET 页面 |
| `app/routers/api_posts.py` | 已接入帖子创建、列表搜索、详情和删除端点；更新端点尚未接入 |
| `app/models/post.py` | 已定义帖子字段及 User.posts / Post.author 一对多 ORM 关系，尚未生成数据库迁移 |
| `app/schemas/post.py` | 定义帖子创建、部分更新和响应 Schema；Router 尚未接入这些契约 |
| `app/services/posts.py` | 已实现单篇查询、标题/正文关键词搜索、创建和部分更新逻辑 |
| `tests/test_posts.py` | 使用内存 SQLite 验证帖子查询、模糊搜索、分页、创建和更新 |
| `app/templates/` | 保存布局、首页、帖子详情和错误页面模板 |
| `app/templates/login.html` | 登录表单页面，仅负责展示，尚未绑定认证页面 Router |
| `app/templates/register.html` | 用户注册表单页面，仅负责展示，尚未绑定注册页面 Router |
| `app/templates/profile.html` | 用户资料展示与编辑页面，尚未绑定资料页面 Router |
| `app/templates/post_form.html` | 帖子新建/编辑共用表单，尚未绑定帖子页面 Router |
| `app/static/` | 保存站点 CSS、主题脚本和前端依赖 |
| `app/media/profile_pics/` | 头像文件的预留存储目录 |

## 新功能开发步骤

1. 明确请求字段、响应字段、状态码和不在本次范围内的功能。
2. 阅读相关 Router、Schema、Service、Model、迁移和测试，确认完整调用链。
3. 先更新 Schema 与 Model 设计，确保字段长度、空值和唯一约束一致。
4. 数据库结构有变化时新增 Alembic Migration，不修改已应用的旧迁移。
5. 在 Service 实现业务规则和事务，在 Router 中只处理 HTTP 行为。
6. 为新模块添加中文模块 docstring，为公共类、函数、端点和复杂配置添加中文说明。
7. 使用隔离测试数据库覆盖成功路径及相关的冲突、404、422 等失败路径。
8. 更新接口文档、数据库文档和本文的文件用途及调用链。
9. 执行静态检查、目标测试、完整测试和迁移检查，再报告结果。

## 验证命令

```powershell
uv run ruff check app migrations tests
uv run pytest -q
uv run alembic current
uv run alembic check
```

测试必须通过 `app.dependency_overrides` 替换 `get_db`，避免操作开发或生产数据。`alembic check` 用于确认 ORM MetaData 是否还有未生成的迁移差异。

## 文档更新要求

每次交付至少检查以下内容：

- 新增、修改或删除了哪些关键文件。
- 每个文件为什么存在，归属哪一层。
- 请求从 Router 到数据库的调用链是否变化。
- 是否改变公开 API、环境配置或数据库结构。
- 新增迁移的升级和回退行为。
- 实际执行了哪些验证命令，哪些检查没有执行及原因。

代码中的注释用于解释局部设计和参数副作用；本文用于解释模块关系和完整开发过程，两者不能互相替代。

## 2026-07-27 前端视觉优化

- `app/templates/layout.html`：优化导航品牌、中文操作文案，并增加轻量页脚；登录和注册仍使用原有模态框，认证流程未改变。
- `app/templates/home.html`：增加文章区标题，限制摘要长度，将英文占位侧栏替换为博客说明和文章入口。
- `app/static/css/site.css`：统一明暗主题色、字体层级、边框、阴影、表单、内容卡片和响应式布局。

本次仅修改展示层，不改变 Router、API、JWT、数据库结构或前端 AJAX 请求链。

## 2026-07-27 文章编辑预览

- `app/templates/post_form.html`：在文章编辑表单中增加“预览文章”按钮和大尺寸预览模态框。预览不会提交或保存文章。
- `app/static/js/posts.js`：点击预览时读取编辑器当前 Markdown，并通过 Toast UI Viewer 渲染；重复预览复用同一个 Viewer 实例。
- `app/static/css/site.css`：补充预览标题、空内容提示、操作按钮及移动端样式。
- `tests/test_pages.py`：检查新建和编辑页面包含预览入口及 Viewer 容器。

前端调用链为：点击“预览文章” → 读取标题和 `editor.getMarkdown()` → Toast UI Viewer 渲染 → 代码块装饰。该过程不发送 AJAX 请求，也不改变数据库；最终保存仍沿用原有帖子 AJAX 请求。

### Markdown 编辑器宽度约束

`app/static/css/site.css` 将编辑面板最大宽度设置为 `1080px`，并为 Toast UI 的编辑器外层和 Markdown 双栏补充 `min-width: 0`，防止第三方组件的内部最小宽度撑破编辑卡片。外层面板不裁剪溢出内容，避免工具栏或富文本区域被遮挡。`app/static/js/posts.js` 在小于 768px 的窄屏上使用标签式预览，桌面端仍保留 Markdown 与预览并排显示。

## 2026-07-27 统一异常处理

| 文件 | 功能 |
| --- | --- |
| `app/exception_handlers.py` | 区分页面与 `/api/` 请求，集中处理 HTTP、参数校验和未知服务器异常 |
| `app/main.py` | 在应用组装阶段注册统一异常处理器 |
| `app/templates/error_401.html` | 未登录或身份失效页面 |
| `app/templates/error_403.html` | 已登录但权限不足页面 |
| `app/templates/error_404.html` | 页面或资源不存在页面 |
| `app/templates/error_500.html` | 未知服务器异常页面，不显示内部错误细节 |
| `app/static/js/api.js` | 从统一 `error` 对象读取接口错误，同时兼容旧 `detail` 格式 |

页面异常链路：Router 或框架抛出异常 → `app/exception_handlers.py` 判断为页面请求 → 选择对应模板 → 保留原 HTTP 状态码返回 HTML。`401` 表示身份未确认，`403` 表示身份有效但权限不足。

API 异常链路：`/api/` Router、依赖或参数校验抛出异常 → 统一处理器转换为以下 JSON。未知 `500` 会在服务端日志记录完整调用栈，但响应只返回通用提示，避免泄露代码、数据库或凭据信息。

```json
{
  "success": false,
  "code": 40401,
  "message": "Post not found",
  "data": null,
  "meta": {
    "requestId": "949dbb1e-8e60-4f43-a3b7-519fcf8b43f4",
    "timestamp": "2026-07-28T00:00:00+00:00"
  },
  "errors": null
}
```

参数校验失败时 `errors` 是由 `field`、`message`、`type` 组成的字段错误数组；其他异常通常为 `null`。错误业务码采用“HTTP 状态码 × 100 + 1”的基础规则，例如未认证为 `40101`、不存在为 `40401`、参数校验失败为 `42201`。HTTP 状态码仍保持 `401/404/422` 等真实语义，不能全部改为 200。

## 2026-07-28 统一 API 成功响应

所有业务 API 成功响应使用以下结构：

```json
{
  "success": true,
  "code": 0,
  "message": "ok",
  "data": {},
  "meta": {
    "requestId": "949dbb1e-8e60-4f43-a3b7-519fcf8b43f4",
    "timestamp": "2026-07-28T00:00:00+00:00"
  }
}
```

| 文件 | 功能 |
| --- | --- |
| `app/schemas/api.py` | 定义泛型 `ApiSuccess[T]` 和 `ResponseMeta`，让 OpenAPI 保留 `data` 的具体类型 |
| `app/api_responses.py` | 统一构造成功响应，写入请求 UUID 和带时区 UTC 时间 |
| `app/main.py` | 请求进入应用时生成 UUID，并通过 `X-Request-ID` 响应头返回 |
| `app/routers/api_auth.py` | 登录、刷新和退出返回统一结构；退出由无正文的 204 改为 200 |
| `app/routers/api_posts.py` | 帖子、列表、图片上传和删除返回统一结构；删除改为 200 |
| `app/routers/api_users.py` | 用户增删改查返回统一结构；删除改为 200 |
| `app/static/js/api.js` | AJAX 公共层自动解包 `data`，业务脚本无需感知响应信封 |

成功响应链路：请求进入中间件生成 `request.state.request_id` → Router 调用 `success_response()` → 泛型 Schema 校验业务数据 → FastAPI 序列化响应 → 中间件写入相同的 `X-Request-ID` Header。

三个 API Router 还会复用 `API_ERROR_RESPONSES`，使 Swagger 中的常见失败状态引用 `ApiFailure`，避免运行时已经统一、OpenAPI 文档却仍展示 FastAPI 默认错误结构。页面 Router 不使用该声明，继续返回 HTML 错误页。

Service 返回 SQLAlchemy ORM 对象时，Router 会先调用 `PostResponse.model_validate(post)` 或 `UserResponse.model_validate(user)` 转为公开响应 Schema，再传给 `success_response()`。这一步利用 `from_attributes=True` 读取 ORM 属性，同时保证 `ApiSuccess[PostResponse]` 等泛型返回类型与实际数据一致，并阻止 ORM 内部字段进入响应。

`POST /api/auth/token` 是博客前端使用的统一响应接口。Swagger OAuth2 必须读取顶层 `access_token`，因此另外使用不展示在业务 API 文档中的 `POST /api/auth/oauth2-token`；它只服务 OAuth2 协议兼容，认证、密码校验和 Cookie 签发逻辑与业务登录完全复用。

HTTP `204 No Content` 按规范不能携带 JSON 正文，所以用户删除、帖子删除和退出登录改为 `200 OK`，其 `data` 为 `null`。这属于公开 API 契约变化，调用方必须同步更新状态码和数据读取方式。

### 登录状态首屏显示

`app/templates/layout.html` 在 `<head>` 中同步读取本地 Access Token，并在页面正文解析前向 `<html>` 写入 `data-auth-state`。`app/static/css/site.css` 根据该状态直接显示对应的导航操作，避免刷新时先出现“登录/注册”、随后再切换成“发布帖子/退出”的闪烁。`app/static/js/auth.js` 仍负责实际登录、注册和退出行为；首屏标记不是后端授权依据，受保护接口仍必须验证 JWT 与用户权限。

`app/exception_handlers.py` 注册处理器时转换为 Starlette 的 `ExceptionHandler` 类型。这只用于解决该类型同时包含 HTTP 与 WebSocket 签名而造成的 Pylance 联合类型误报，不改变异常处理行为。

## 2026-07-28 首页搜索与分类导航

- `app/templates/layout.html`：把搜索入口放到全站导航栏右侧，并增加 VuePress 风格的标题搜索弹窗；支持点击入口与 `Ctrl/Command + K` 打开。
- `app/static/js/search.js`：对关键词输入执行 300ms 防抖，通过 `GET /api/posts/search` 获取最多 8 条标题结果；新请求会取消旧请求，弹窗关闭时也会取消待执行或进行中的请求，避免过期结果更新界面。
- `app/templates/home.html` 与 `app/static/css/site.css`：移除首页内嵌搜索框，增加分类导航、当前分类状态、文章分类标签和响应式布局，并统一导航、弹窗、文章列表与侧栏的简洁现代视觉。
- `app/routers/pages.py`：首页读取分类列表，并将 `category` 查询参数传入帖子 Service，向模板提供当前选中分类。
- `app/routers/api_posts.py`、`app/schemas/post.py` 与 `app/services/posts.py`：列表接口支持分类筛选；新增仅匹配标题的轻量搜索接口，返回文章 `id` 与 `title`，供弹窗跳转详情页使用。
- `tests/test_pages.py`、`tests/test_posts.py` 与 `tests/test_posts_api.py`：覆盖首页分类展示与筛选、标题搜索不匹配正文、分页和参数校验。

搜索调用链：输入关键词 → 300ms 防抖 → `GET /api/posts/search?keyword=...&limit=8` → Router 校验参数 → Service 按标题查询 → 返回 `id/title` → 点击结果进入 `/posts/{id}`。分类调用链：点击分类 → `GET /posts?category={slug}` → 页面 Router → 帖子 Service 按分类 slug 筛选 → 服务端渲染文章列表。

本次没有修改 ORM Model、数据库结构、迁移、认证方式或环境配置。目标验证命令为：

```powershell
uv run pytest tests/test_pages.py tests/test_posts_api.py tests/test_posts.py -q
uv run ruff check app tests
```

## 2026-07-28 修复帖子分类迁移

- `migrations/versions/145903a6328c_add_post_category_stable.py`：分类表创建支持 SQLite 失败后重试；为 `posts.category_id` 增加非空字段、索引和 `RESTRICT` 外键时使用 Alembic batch 模式，避免 SQLite 不支持直接新增外键约束。
- `fastapi_blog.db`：迁移前的 12 条帖子没有分类关联。按本次维护要求删除这些旧帖子后，数据库已升级到 `145903a6328c (head)`；用户和刷新会话数据未删除。
- `fastapi_blog.before_category_cleanup_20260728_180555.db`：删除旧帖子前通过 SQLite Backup API 创建的本地恢复备份。

失败链路为：迁移先创建 `categories` 表 → 尝试向包含历史记录的 `posts` 表新增无默认值的非空 `category_id` → SQLite 拒绝变更，但已创建的表无法随事务完整回滚 → 再次执行迁移时先报 `table categories already exists`。修复后，空的历史帖子表可通过 batch 模式重建并增加分类约束，迁移也能识别失败遗留的分类表后继续。

验证命令：

```powershell
$env:PYTHONPATH = (Get-Location).Path
uv run alembic upgrade head
uv run alembic current
uv run alembic check
uv run ruff check migrations/versions/145903a6328c_add_post_category_stable.py
```

验证结果：数据库版本为 `145903a6328c (head)`，`alembic check` 未发现新结构差异；`posts.category_id` 为非空列，分类外键删除策略为 `RESTRICT`，帖子数量为 0。

## 2026-07-31 会话状态、提交锁与评论学习文档

- `GET /api/auth/me`：页面启动时验证缓存 Access Token 并返回当前用户权限；启动校验不静默刷新，Token 失效会清理本地状态并打开登录模态框。
- `app/static/js/ui.js`：统一按钮 loading、固定宽度和重复提交锁；登录、注册、退出、搜索、资料保存、发帖和评论复用该工具。
- `app/static/js/api.js` 与 `app/static/js/auth.js`：最终 `401` 统一触发登录事件；导航首屏使用缓存 Token，后端验证后再设置管理员状态，避免退出按钮闪烁。
- `app/templates/post_form.html`：编辑区域默认隐藏，只有 `/api/auth/me` 确认管理员后显示；未登录弹登录框，普通用户显示 403。Bearer Token 保存在 localStorage，普通页面 GET 不携带 Authorization Header，因此该拦截由页面启动校验完成，真正写入仍由 API 的 `AdminUser` 依赖保护。
- `docs/评论模块代码逐层解析.md`：按 Model、Schema、Service、Router、连接管理器和前端顺序，详细解释评论字段、函数、状态变量、通信消息和调试断点。

当前开发数据库只读检查显示 `admin2.is_admin=True`。如果运行中的页面仍返回 `Admin access required`，应检查浏览器当前 Token 对应的 `/api/auth/me` 响应，并确认应用进程连接的是同一数据库且已重启加载最新代码。

## 2026-07-31 评论 WebSocket 交互流程文档

- `docs/评论WebSocket交互流程.md`：从帖子详情页的 `connect()` 开始，沿前端事件、Router 认证、房间管理器和断线清理完整说明评论 WebSocket 生命周期。
- 文档明确区分 `accept()`、认证、`register()`、`broadcast()` 和 `disconnect()`，并分别给出入房间、评论广播、正常关闭与异常断线时序。
- `register()` 只在认证成功后触发；`broadcast()` 只在评论事务提交成功后触发；Router 的 `finally` 负责常规清理，Manager 还会在广播失败时清理失效连接。

本次只增加学习文档，不改变 API、WebSocket 消息协议、数据库结构、迁移或运行时代码。

## 2026-07-31 前端 JavaScript 教学注释

为便于按执行链阅读，`app/static/js/` 中项目自有脚本补充了中文模块说明、入口分区、状态变量、事件流、异步请求和失败处理注释；本次未修改 JavaScript 行为。第三方的 jQuery 与 Bootstrap 压缩文件不做修改。

| 文件 | 建议阅读主线 |
| --- | --- |
| `api.js` | 业务请求 → Bearer Token → 401 刷新 → 重试 → 登录事件 |
| `auth.js` | DOM ready → 表单事件 → 登录/注册/退出 → `/auth/me` 启动校验 |
| `comments.js` | HTTP 历史 → WebSocket 连接/认证 → 评论提交/广播 → 断线/重连 |
| `forms.js` | 委托监听表单 → 搜索 HTML 局部替换或资料 PATCH → 页面反馈 |
| `posts.js` | 详情 Viewer 或编辑器初始化 → 图片上传/预览 → POST/PATCH 保存 |
| `search.js` | 输入事件 → 300ms 防抖 → 取消旧请求 → 版本校验 → 渲染标题结果 |
| `theme.js` | 恢复本地偏好 → 解析实际主题 → 用户切换或系统主题变化 |
| `ui.js` | 加载锁检查 → 保存按钮原内容/宽度 → loading → 请求结束后恢复 |

这些注释解释框架约定、状态含义和“为什么这样执行”，不会代替后端身份校验、Schema 校验或提交锁测试。

## 2026-07-31 用户菜单与管理员后台

- `app/templates/layout.html`、`app/static/js/auth.js`、`app/static/css/site.css`：登录后右上角显示当前用户头像；菜单展示头像、昵称、资料入口、管理员后台、改密和退出。内容来自 `/api/auth/me`，缓存 Token 只控制首屏可见性。
- `POST /api/users/me/avatar`：只为当前认证用户保存头像；校验文件魔数和 5 MB 上限，磁盘保存随机文件名，数据库只记录文件名。
- `POST /api/auth/password`：接收当前密码、新密码和确认密码；Service 验证旧密码后才生成新 Argon2 哈希并提交。
- `app/templates/admin/` 与 `app/static/js/admin.js`：后台使用独立侧栏 layout，提供概览、用户管理和帖子管理页面。HTML 只提供外壳，每个数据请求仍由后端认证授权。
- `app/routers/api_admin.py`：提供仅管理员可用的用户列表、新增、更新和删除接口；公开注册不能提交管理员角色，并禁止管理员从后台删除自己或撤销自己的管理员身份。
- 帖子后台复用现有 `/api/posts` 查询以及受 `AdminUser` 保护的新增、修改和删除接口，不重复实现另一套帖子业务。

关键调用链：

```text
页面启动 → GET /api/auth/me → 填充头像菜单 / 判断 is_admin
管理员后台 → GET /api/admin/users → AdminUser → User Service → users 表
修改密码 → POST /api/auth/password → CurrentUser → 验证旧哈希 → 写入新哈希
上传头像 → POST /api/users/me/avatar → 文件签名校验 → 磁盘 → image_file
```

本次没有新增数据库字段或迁移。头像继续复用已有 `users.image_file`，帖子管理复用已有帖子模型和接口。

### 导航与发帖工作区调整

- 前台导航不再单独显示“发布帖子”按钮；管理员入口移动到头像菜单，并且仍由 `/api/auth/me` 返回的 `is_admin` 控制显示。
- 主题切换位于头像左侧；后台顶栏使用同一套 `theme.js` 和 `blog-theme` 本地偏好，前后台切换时主题保持一致。
- `app/templates/post_form.html` 改为继承 `admin/layout.html`。新建和编辑文章在后台工作区展示，后台校验完成前编辑内容保持隐藏，最终 POST/PATCH 仍由 `AdminUser` 依赖授权。
- 主按钮的默认、悬停、按下、禁用和 loading 状态统一使用站点绿色主题；loading 只降低透明度并展示进度，不切换到 Bootstrap 默认蓝色。

### 单按钮主题切换

- 前台导航和后台顶栏的主题控件改为 38px 圆形纯图标按钮，不显示 `Auto/Light/Dark` 文字，也不再打开下拉菜单。
- 浅色页面显示月亮 SVG，表示点击后进入深色；深色页面显示太阳 SVG，表示点击后进入浅色。SVG 使用 `currentColor`，随所在导航自动调整颜色，无需额外图片文件。
- `theme.js` 在点击后直接切换并保存明确的 `light` 或 `dark`；首次没有手动偏好时仍根据操作系统主题初始化。
- 按钮的 `title` 和 `aria-label` 会根据当前主题更新为“切换到深色主题”或“切换到浅色主题”，同时满足鼠标提示和读屏说明。

### 头像菜单操作项对齐

- “发布新帖子”已并入头像菜单的双列操作网格，与“编辑个人信息”“进入我的后台”“修改密码”使用相同的高度、内边距、字号和悬停反馈。
- 发布入口不再使用绿色主题背景，避免普通导航操作被误解为主提交按钮；管理员可见性仍由 `data-auth-admin` 和 `/api/auth/me` 返回的权限控制。
